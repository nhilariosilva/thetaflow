Dev Log: April 15, 2026

- Added force_training_validation toggle: Introduced a boolean argument in the training routine that forces the early stopping and learning rate schedulers to evaluate the training data, even if validation data is provided. This is primarily a backend utility to support the fine-tuning step, ensuring the optimizer strictly targets a local maximum on the training manifold.

- Decoupled Fine-Tuning Hyperparameters: Added fine-tune-specific arguments for warmups, patience, and tolerances. When transitioning to fine-tuning, the geometry of the model drastically collapses from thousands of deep learning weights down to a few dozen statistical parameters. Isolating these hyperparameters ensures the optimizer navigates this strictly convex space correctly to achieve the stable Hessian required for valid standard errors.

- Enforced Full-Batch Gradient Descent for Fine-Tuning: thetaflow now automatically intercepts and overrides user batching arguments (fixing train_batch_size and gradient_accumulation_steps to 1 batch) when fine_tune = True. This eliminates stochastic mini-batch noise, protecting users from accidentally halting on a sloped gradient and generating mathematically invalid covariance matrices.

- Resolved the "Ghost Seed" Initialization Bug: Moved the global seed and op-determinism lockdown directly into the ModelNN.__init__ method, executing right before the layers and independent parameters are built. This fixes an issue where a fresh kernel's first execution yielded a different random state than all subsequent reruns, guaranteeing perfect weight reproducibility from the exact moment the object is instantiated.

- Included training loss into final epoch evaluation, so that the loss history for both train and validation do not depend on training randomness, since there they
are evaluated with training = False.

- Included a restriction that tracks the best metric in training only after the early_stopping_warmup has been overcame. That avoids that an early stage degeneracy that was unstable, but might have presented low losser values get selected over a stable, slightly higher loss region reached after model proper convergence

- Fixed problems in training weight recovering. Now the final model weights correspond to the best observed epoch after the warmup period

- Implemented a return variable in the training routine to track the best metric and the epoch where that happened

Dev Log: April 17, 2026

- Added self.best_weights initialization in pre_train_model function (BUG)
- Switched best weights from tracking self.trainable_variables to track self.variables. That way it can track variables such as layer normalization constants in custom neural network layers

Dev Log: April 22, 2026

- Fixed the older version of variable_function_covariance to match current covariance calculations, such as acknowledging that self.covariance_output returns a tuple now

Dev Log: April 26, 2026

- In order to fully implement huge datasets, reprogrammed every aspect of input data handling and training configurations. Now all data is handled in the background by tf.data.Dataset objects,
ensuring that not necessarily all data should be loaded into memory.
- The implementation involved the functions "get_covariances", "config_training", "pre_train_model", "train_model" and "_compiled_training_loop_optimized"
- The package now accepts preprocessed tf.data.Dataset structures as well as full data, disposed in the form of arrays. By allowing Datasets, we consider cases where data must be loaded
dynamically during training, because otherwise memory can't handle so much size.

Dev Log: April 27, 2026

- Fixed elapsed time and training speed calculations for all possible training execution scenarios (was unable to track time when using GPU with full determinism on)
- Updated the prints during training to show the speed estimates and the total training time
- Fixed get_covariances function to obtain the matrix optimally using the C++ flux (Large datasets were exploding memory usage when reaching this function after training)

Dev Log: April 28, 2026

- The complete update with tf.data.Datasets added an extreme overhead when data is simple and fully loaded into RAM. To fix it, I split the training process into _compiled_training_loop_rawdata and _compiled_training_loop_dataset. Both functions handle training data differently, depending if user input was a tf.data.Dataset object or Numpy arrays
- Added option of having different number of epochs during fine-tuning than during training

Dev Log: April 29, 2026

- Added the tf.Dataset memory friendly processing approach for functions variable_function_covariance, summary, predict, covariance_output, evaluate_loss. Now, these functions support both raw data structures and tf.Datasets, handling memory accordingly
- Fixed bug where finetune training hyperparameters were not being considered for the _compiled_training_loop_rawdata call in train_model

Dev Log: May 8, 2026

- Inside the get_covariances function, fixed a bug that led to an error when using a model with independent parameters only (no neural network components)
- In the same case, Warning about neural network being used was showing up even when self.neural_network_use was False. Fixed now
- Fixed minor bug in summary, that made so that the point prediction of independent parameters were wrong when having other, nn variables in the model

Dev Log: May 11, 2026

- Changed training function so that fine tuning hyperparameters are the same from the training step, if not provided

Dev Log: May 28, 2026

- Added reshuffle_each_iteration = True and seed = self.seed in all calls to tf.data.Dataset shuffle to ensure reproducibility

Dev Log May 30, 2026

- Updated verification for batches in config_training. Now it is more robust to detecting whether tf.data.Dataset was indeed previously batched or not
- Now the package verifies whether a given batched tf.data.Dataset trully corresponds to the original input_dim. If now, it raises an error that the user possibly batched twice, or is considering a Dataset completely different from what the input dimension specifies
- Following the change above, we also set the input_dim argument as mandatory from now

Dev Log June 2, 2026

- Fixed buffer_size double verification in config_training. If user passes value lesser than training size, explicitly set the buffer to 3 times the training size, to avoid OOM problems

Dev Log June 11, 2026

- Improved the progress bar to point both training and validation average losses (improve interpretability of convergence, instead of total loss), as well as what is the metric of interest for reduce_learning_rate and early_stopping (the one indicated in the best metric is the one chosen for model evaluation).
- Fixed a bug in the training routine for tf.data.Dataset data inputs where the evaluation metrics took into account the regularization values. Ideally, the regularization should be applied just during the gradient calculations during training. We should not early stop based on a regularized metric (Note that in order for a model to improve, it may be naturally necessary to increase its weights to fit the data. If we keep the regularization loss for early stopping, the model will likely stop training too early)
- Fixed a source of confusion for early stopping. If contribution to the loss is less than delta value, it gets completely ignored by the model, leading the best reported loss to be greater than the model final loss. The delta parameter should regulate only whether that loss improvement is relevant to completely reset the patience counter. Now, if model keeps improving less than delta for the next iterations, we will early stop at the same epoch as before (i.e. that improvement will not be enough to reset the patience), however, the best metric and weights associated to that best metric will still be saved at the actual point where they were observed

Dev Log June 13, 2026

- Fixed a bug where the corrections in early stopping in the previous Dev Log were not applying for dataset training
- Created a new input variable early_stopping_min_delta that separates loss improvement evaluation for early stopping and for reduce learning rate

Dev Log June 30, 2026

- Fixed a bug when applying gradients that led it to work only when clipnorm was added to optimizer (Very specific bug)

Future fixes:
- Fix model structure so that it natively supports likelihood loss functions based on tf.reduce_mean instead of tf.reduce_sum. That makes training much more stable and easy to interpret (Not difficult to implement). Require testing how Regularization is working now and how it will relate to this. Apparently, the current implementation with regularizers is broken, given the huge dimension of the loss
- Implement routine that detects if a model has no neural network components and optimize its parameters according to a standard double order optimizer (such has BFGS) as a standard procedure. The user may still deliberatly configure it to use gradient descent if they want to though, as some more complex models may present optimization problems even without neural networks.
- 




